spring:
  datasource:
    url: jdbc:h2:mem:kioskdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true

management:
  server:
    port: 8080